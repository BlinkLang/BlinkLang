// An example on global and local functions
fun globalFunction() {
    print "Oh hey look, I'm a global function!";
    print " ";

    fun localFunction() {
        print "I'm a poor little local function!";
    }

    // This makes the global function invoke the local function when the global one is called
    // If you remove this line, you should only get the print from the global function
    localFunction();
}

globalFunction();